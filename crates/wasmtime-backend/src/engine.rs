//! Wasmtime engine + Backend impl.

/// Placeholder for the Wasmtime backend.
///
/// Tasks 22-27 wire the real impl: float-ban lint, capability-gated
/// linker, deterministic helpers, fuel + memory caps, and the
/// `ComponentInstance` impl.
pub struct WasmtimeBackend;
