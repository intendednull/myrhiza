**Date:** 2026-05-26
**Status:** active
**Subject:** plan B-6 — poll app v1 implementation
**Parent spec:** [docs/specs/2026-05-26-b-6-poll-app-design.md](../specs/2026-05-26-b-6-poll-app-design.md)

# Plan B-6 — poll app v1 implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Spec at [`docs/specs/2026-05-26-b-6-poll-app-design.md`](../specs/2026-05-26-b-6-poll-app-design.md) is the design contract; this plan is the execution order. Steps use checkbox (`- [ ]`) syntax for tracking.

## Goal

Ship the v1 poll app (second MVP demo app called for in [mvp.md §15.2](../specs/2026-05-09-myrhiza-master-design/mvp.md)) as a four-component WASM bundle (state-apply + state-propose + interaction + manifest) exercising multi-author voting, a creator-only `EndPoll` permission gate, the first fixture that tolerates non-empty `deps`, and the first non-empty use of `peer_state` in the B-7 harness. Closes [implementation.md §20 item 16](../specs/2026-05-09-myrhiza-master-design/implementation.md) per the [post-B-4 gap analysis](../reports/2026-05-21-mvp-gap-analysis.md). 13 tasks T0–T12; total estimate 2.5–3 days per spec §8. Path-A (`examples/poll/`) is preferred if B-8 has landed; Path-B (`tests/fixtures/poll-*/`) is the contingency, chosen mechanically at T0.

## Pre-flight

- Worktree: `.claude/worktrees/b-6-poll-app` (already exists; verified `git status` clean against `feat/b-6-poll-app`)
- Branch: `feat/b-6-poll-app`
- Base: `main` (last commit `5999c9e` — B-7 + criterion 3 + interaction harness landed)
- B-8 dependency (path selection): resolved at T0 — see "Path selection" below.
- Fixture toolchain: `rustup target add wasm32-unknown-unknown` + `wasm-tools` already in use by `just build-fixtures` per [`Justfile`](../../Justfile).

## Path selection — Path-A vs Path-B (resolved at T0)

Per spec §3 the technical content is identical under both paths; only file layout differs. The plan defaults assume Path-B because B-8 (`examples/` + `crates/sdk/`) has not landed as of base commit `5999c9e` — neither `examples/` nor `crates/sdk/` exists in the tree. If B-8 lands between this plan's authoring and impl-start, T0 will switch every subsequent task's "Files touched" path from `tests/fixtures/poll-*/...` to `examples/poll/src/{state,propose,interaction}.rs` mechanically — the impl content is unchanged.

The whole plan is written against Path-B paths. Every task that touches a Path-B path includes a "Path-A path:" note showing the equivalent under Path-A; T0 records the selection in the plan-coverage report so reviewers can see which path was taken.

## Tasks

---

### T0 — Path-A vs Path-B contingency check + workspace scaffold + WIT stubs

**Spec ref:** §3.1, §3.2.

**Subject:** decide layout, scaffold the three fixture crates (or `examples/poll/` workspace member), copy the WIT files used by counter into the new fixture dirs, register them in workspace.exclude (Path-B) or workspace.members (Path-A).

**Acceptance:** `cargo check --workspace` is clean (the new fixture dirs do not break workspace gates); `ls tests/fixtures/poll-state-apply tests/fixtures/poll-state-propose tests/fixtures/poll-interaction` (Path-B) or `ls examples/poll/src` (Path-A) shows scaffolded crates with placeholder lib.rs / WIT files; `just build-fixtures` is NOT yet expected to succeed (no implementations).

**Path selection check:**

```bash
# Run this FIRST. If both directories exist and contain Cargo.toml, B-8 has landed → Path-A.
if [ -f examples/poll/Cargo.toml ] || [ -d crates/sdk ]; then
    echo "B-8 detected → Path-A"
else
    echo "B-8 not landed → Path-B"
fi
```

**Files touched (Path-B):**
- Create: `tests/fixtures/poll-state-apply/Cargo.toml` (mirrors `tests/fixtures/counter-state-apply/Cargo.toml` verbatim with `name = "poll-state-apply-fixture"`)
- Create: `tests/fixtures/poll-state-apply/wit/world.wit` (copy of `tests/fixtures/counter-state-apply/wit/world.wit`)
- Create: `tests/fixtures/poll-state-apply/.gitignore` (single line: `/target`)
- Create: `tests/fixtures/poll-state-apply/src/lib.rs` (placeholder — just the bump-allocator + panic handler + `wit_bindgen::generate!` + empty `apply` stub returning `Reject("unimplemented")`)
- Create: `tests/fixtures/poll-state-propose/Cargo.toml` + `wit/world.wit` + `.gitignore` + `src/lib.rs` (analogous; mirror counter-state-propose)
- Create: `tests/fixtures/poll-interaction/Cargo.toml` + `wit/world.wit` + `.gitignore` + `src/lib.rs` (analogous; mirror counter-interaction)
- Modify: root `Cargo.toml` — append three entries under `[workspace.exclude]` (after the counter fixtures): `"tests/fixtures/poll-state-apply"`, `"tests/fixtures/poll-state-propose"`, `"tests/fixtures/poll-interaction"`.
- Modify: `Justfile` — extend the `build-fixtures` recipe with three new `_build-fixture` lines:
  - `(_build-fixture "poll-state-apply" "poll_state_apply_fixture" "state-apply") \`
  - `(_build-fixture "poll-state-propose" "poll_state_propose_fixture" "state-propose") \`
  - `(_build-fixture "poll-interaction" "poll_interaction_fixture" "interaction") \`
  - Update the `@echo "Built 8 fixtures..."` line to `"Built 11 fixtures into tests/fixtures/built/"`.

**Path-A paths (use these if Path-A is selected):**
- Replace the three `tests/fixtures/poll-*` directories with a single `examples/poll/` workspace member containing `Cargo.toml`, `manifest.toml`, `src/{state,propose,interaction}.rs`, and the SDK's build helper to produce the three .wasm artifacts. Skip the `workspace.exclude` change; instead add `"examples/poll"` to `[workspace.members]`. Skip the Justfile changes (B-8's build helper subsumes them). Note in the impl report which path was taken.

**Implementation notes:**
- The placeholder `apply` MUST return `Reject("unimplemented")` not `Accept(vec![])` — otherwise mid-T1 tests can pass spuriously.
- Mirror counter-state-apply's `#![no_std]` + `#![no_main]` + bump-allocator block verbatim. Copy from `tests/fixtures/counter-state-apply/src/lib.rs:1-89` (the boilerplate ending just before `wit_bindgen::generate!`).
- Re-read spec §3.1 + §3.2 (Path-A vs Path-B contingency) and §4.5.1 / §4.5.2 / §4.5.3 (component shapes) before touching files.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
cargo check --workspace --all-targets
ls tests/fixtures/poll-state-apply/{Cargo.toml,wit/world.wit,src/lib.rs,.gitignore} \
   tests/fixtures/poll-state-propose/{Cargo.toml,wit/world.wit,src/lib.rs,.gitignore} \
   tests/fixtures/poll-interaction/{Cargo.toml,wit/world.wit,src/lib.rs,.gitignore}
grep -c '_build-fixture "poll-' Justfile  # expect: 3
```

**Risks:** copying counter's WIT files verbatim is intentional — poll reuses the same four worlds. If the worlds differ (they should not, per spec §4.5.4 "no WIT changes"), revisit at T0 before going further.

---

### T1 — poll-state-apply skeleton (decoder + match scaffolding, no real logic)

**Spec ref:** §4.5.1 (apply pseudocode), §6.3 (non-empty-deps tolerance).

**Subject:** stand up the byte-offset decoder for the canonical bincode envelope adapted from counter, including the non-empty-deps path. Return `Reject("unimplemented")` from every match arm — no payload logic yet. Build a wasm artifact via `just build-fixtures`.

**Acceptance:** `just build-fixtures` succeeds; `tests/fixtures/built/poll-state-apply.wasm` exists and has the component-model magic header.

**Files touched:**
- Modify: `tests/fixtures/poll-state-apply/src/lib.rs` — replace the placeholder with the full envelope decoder (parse `author`, `seq`, `deps_len`, skip past deps array, `payload_len`, `payload`) and a `match payload[0] { ... }` discriminator switch that returns `Reject("unimplemented")` from every arm including the genesis branch. Drop the counter fixture's `deps_len != 0` hard reject — poll MUST tolerate non-empty deps (spec §6.3, ~15-LOC offset-decoder change vs counter).

**Path-A path:** modify `examples/poll/src/state.rs` instead.

**Implementation notes:**
- Re-read spec §6.3 ("first fixture that tolerates non-empty deps") and the byte-offset table at `tests/fixtures/counter-state-apply/src/lib.rs:100-147`.
- The deps array is `deps_len` (u64 BE) followed by `deps_len * 40` bytes (each EventId is a length-prefixed 32-byte hash → 8 + 32 = 40 bytes). PAYLOAD_LEN_OFFSET becomes `HLC_OFFSET + 12 + (deps_len as usize) * 40` — computed dynamically, NOT a compile-time constant.
- The genesis discriminator is `seq == 1 && prior_state.is_empty()` per counter (counter-state-apply.rs:191).
- T1 is structural-only; tests do not yet pass. T1's purpose is to verify the decoder is well-formed and `just build-fixtures` produces a component-model binary.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
ls -la tests/fixtures/built/poll-state-apply.wasm  # exists, non-empty
# Sanity: component-model magic header (0x00 0x61 0x73 0x6D 0x0D 0x00 0x01 0x00 == "\0asm" + component model version)
head -c 8 tests/fixtures/built/poll-state-apply.wasm | xxd | grep -q '00 61 73 6d 0d 00 01 00' && echo OK
```

**Risks:** the non-empty-deps decoder is the load-bearing novel surface. If `payload_len` ends up at the wrong offset, every subsequent test will fail with confusing `Reject("payload extends past event bytes")` errors. Sanity-check by manually constructing a 0-deps envelope and a 1-dep envelope (the latter via `EventBuilder` in T3) and printing the byte offsets.

---

### T2 — poll-state-apply CreatePoll/Vote/EndPoll logic + canonical encoder

**Spec ref:** §4.2 (data model), §4.3 (event vocabulary), §4.5.1 (apply pseudocode), §4.1.2 (last-vote-wins + BTreeMap discipline), §4.1.3 (EndPoll creator-only gate).

**Subject:** implement the three discriminator arms (`0x00 CreatePoll`, `0x01 Vote`, `0x02 EndPoll`) plus the genesis path. Write the hand-rolled canonical encoder for `PollState` (`creator: [u8;32]` + `options: Vec<String>` + `votes: BTreeMap<[u8;32], u32>` + `ended: bool` — ~30 LOC per spec §4.5.1 last paragraph). Implement `state_digest` as identity.

**Acceptance:** the fixture builds cleanly; manual sanity (deferred to T3 tests) — `just build-fixtures` continues to succeed; `cargo test --workspace` continues to pass (no new tests yet, but existing ones must not break).

**Files touched:**
- Modify: `tests/fixtures/poll-state-apply/src/lib.rs` — full `apply` implementation per spec §4.5.1 pseudocode. Constants `MAX_OPTIONS = 16` and `MAX_OPTION_LABEL_LEN_BYTES = 64` per spec §4.2. The canonical encoder iterates the BTreeMap in key order (sorted-by-key-bytes is the structural guarantee — see spec §6.1 mitigation).

**Path-A path:** modify `examples/poll/src/state.rs` instead.

**Implementation notes:**
- Re-read spec §4.2 (PollState shape), §4.3 (event vocabulary including the wire layout note), §4.5.1 (encoding-discipline call-out about float-free hand-rolled encoder).
- `state_digest` is identity (return the input slice verbatim) per spec §4.5.1 last code block, mirroring counter's `tests/fixtures/counter-state-apply/src/lib.rs:239-244` and echo's `tests/fixtures/echo-state-apply/src/lib.rs:217`.
- The encoder for `Vec<String>` uses u64-BE length prefixes per spec §4.5.1 "encoding-discipline call-out".
- For `BTreeMap<[u8;32], u32>`: iterate the entries (BTreeMap iter yields key-sorted), write `u64-BE len`, then for each `(author, opt_idx)` write `32 bytes author` + `u32-BE opt_idx`. DO NOT use any HashMap, DO NOT sort separately — BTreeMap structural ordering is the determinism guarantee per spec §6.1.
- The fixture is `#![no_std]` + `no serde`; do NOT pull in `serde`/`bincode` — they drag in float-Display paths per the comment block at `tests/fixtures/counter-state-apply/src/lib.rs:91-130` (and §4.5.1 encoding-discipline call-out cites this).
- The `EndPoll` arm: read `state.creator` from decoded prior_state, read `event.author` from envelope bytes (offsets 0..40 contain `[u8; 8] len-prefix + 32 bytes` per the counter byte-offset table at counter-state-apply.rs:100-104). Compare. Reject with `"EndPoll: not poll creator"` per spec §4.5.1 + §6.2.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
ls -la tests/fixtures/built/poll-state-apply.wasm
cargo test --workspace --no-run --quiet  # tests build (no new tests yet)
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
```

**Determinism callout:** the BTreeMap encoder is the load-bearing determinism property for cross-peer convergence (spec §6.1). Any switch to HashMap here is a CORRECTNESS bug per CLAUDE.md "Determinism is a load-bearing property" — review the encoder block with that lens before committing.

**Risks:** float-Display creep. If `cargo build` for the fixture emits `f32.*` / `f64.*` opcodes (e.g., from any panic-format path), wasm-tools will produce a working component but the kernel's float-ban byte-scan will reject it at instantiation. Spot-check via `wasm-tools dump tests/fixtures/built/poll-state-apply.wasm | grep -E 'f(32|64)\\.' || echo "no float ops"`.

---

### T3 — state-tier unit tests (cases 1–6, 6b, 7–10) in `crates/kernel/tests/poll_state_apply.rs`

**Spec ref:** §4.1.5 (state-tier coverage table), §5.1 (state-tier test infrastructure).

**Subject:** write the 10 state-tier `#[test]` functions per spec §4.1.5 (cases 1–6 + 6b + 7–10) plus the helper functions that build per-case envelopes. Each test instantiates the poll-state-apply WASM via `WasmtimeBackend::instantiate_state_apply` + `StateApplyHandle::new`, builds the event via `myrhiza_kernel::event_builder::EventBuilder`, calls `handle.apply(prior_state, event_bytes)`, asserts the verdict + (on Accept) the canonical-bincode bytes of the resulting `PollState`.

**Acceptance:** all 10 state-tier tests pass. `cargo test -p myrhiza-kernel --test poll_state_apply` is green.

**Files touched:**
- Create: `crates/kernel/tests/poll_state_apply.rs` — the 10 tests + a few helper functions (`build_poll_apply_handle()`, `make_genesis_envelope(seed, founder, options)`, `make_vote_envelope(author, seq, deps, option_index)`, `make_end_poll_envelope(author, seq, deps)`, `encode_poll_state(creator, options, votes, ended)` — the test-side replica of T2's wire encoder used for golden-bytes asserts).
- Modify: `crates/kernel/tests/helpers/mod.rs` — add `pub fn poll_handle() -> StateApplyHandle` that mirrors `counter_handle()` (built via a new test-utils helper `build_signed_poll_bundle()` — see T6).

**Path-A path:** identical paths (kernel tests don't move with B-8).

**Implementation notes:**
- Re-read spec §4.1.5 table (the 10 rows) and §5.1 "state-tier" sub-section. EACH ROW IS ONE TEST FUNCTION — no test combines two rows.
- Test 6b (`vote_replay_out_of_order_converges_to_lex_last`): apply two Vote events from the same author in BOTH orders against the same prior state; assert the final `PollState` bytes are identical. This is the canary for the deps-monotonicity transient anomaly (§6.4b).
- For tests that need a constructed envelope, use `myrhiza_kernel::event_builder::EventBuilder` (the re-homed builder per B-7.0). For tests that need a genesis envelope, the payload is `0x00 ++ <bincode of options>` wrapped in a `GenesisV1` envelope per spec §4.3.
- Tests 7–10 cover structural-validity (option_index out of range; non-genesis CreatePoll rejected; zero options rejected; > MAX_OPTIONS rejected).
- `cargo test -p myrhiza-kernel` already depends on test-utils + the built fixtures dir; the test file picks up `tests/fixtures/built/poll-state-apply.wasm` via the new `helpers::poll_handle()`.
- DO NOT instantiate a `Runtime` here — state-tier tests are pure invocations of `state-apply.apply(prior, event)` per spec §5.1.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
cargo test -p myrhiza-kernel --test poll_state_apply
# expect: 10 passed; 0 failed (cases 1, 2, 3, 4, 5, 6, 6b, 7, 8, 9, 10)
cargo fmt --all -- --check
cargo clippy -p myrhiza-kernel --tests -- -D warnings
```

**Determinism callout:** test 1 (`genesis_create_poll_accepts`) MUST assert the exact canonical-bincode bytes of the initial `PollState`. This is the wire-freeze for poll's state encoding — it's the canary that catches any future "I'll just switch to HashMap" PR per CLAUDE.md "Determinism is a load-bearing property". Spec §4.5.1 last paragraph names this exact assertion.

**Risks:** the encoder must match BYTE-FOR-BYTE between the fixture's encode side and the test-side replica. If they drift, every test fails with mysterious `assert_eq` mismatches. Factor the encoding logic into a small helper and reuse it in the test (keep the no_std fixture code separate, but the test-side replica should be a verbatim mirror).

---

### T4 — poll-state-propose: implementation + MAX_OPTIONS pre-check

**Spec ref:** §4.5.2 (state-propose pseudocode + future-ABI-gap call-out).

**Subject:** implement the three intent discriminators (`0x00 CreatePoll`, `0x01 Vote`, `0x02 EndPoll`) per spec §4.5.2 pseudocode. Reject zero-option / >MAX_OPTIONS at the CreatePoll arm (defense-in-depth — state-apply also checks). The EndPoll arm only checks `state.ended` (cannot check creator identity since propose has no host-import for local author per spec §4.5.2 future-ABI-gap note).

**Acceptance:** `just build-fixtures` succeeds; `tests/fixtures/built/poll-state-propose.wasm` exists with the component-model magic header. (No standalone tests yet — propose is exercised by kernel-tier tests in T7.)

**Files touched:**
- Modify: `tests/fixtures/poll-state-propose/src/lib.rs` — full impl per spec §4.5.2. Reuse counter-state-propose's bump-allocator block verbatim; only the `propose` body differs.

**Path-A path:** modify `examples/poll/src/propose.rs` instead.

**Implementation notes:**
- Re-read spec §4.5.2 in full — propose's `EndPoll` arm intentionally does NOT check creator identity. This is the load-bearing asymmetry with state-apply.
- The propose component reuses the same `PollState` decoder from T2 (`creator` + `options` + `votes` + `ended`) — copy the decoder into the propose crate (small, isolated, the alternative of sharing via a crate dependency drags wit-bindgen incompatibility).
- The future-ABI-gap call-out in §4.5.2 is informational only — DO NOT implement `host.local_author()` in this slice.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
ls -la tests/fixtures/built/poll-state-propose.wasm
cargo fmt --all -- --check
```

**Risks:** decoder duplication between state-apply and state-propose. If T2 changes the canonical encoding, T4's decoder must change in lockstep — flag this in the commit message. (A future refactor could promote the encoder to a small no_std crate; out of B-6 scope.)

---

### T5 — poll-interaction component (text view + dispatch + pluralization + peer_state)

**Spec ref:** §4.1.4 (view shape — all four projections + peer_state contract), §4.5.3.

**Subject:** implement `view(state, peer_state) -> bytes` (UTF-8 text block: poll prompt + status + options with counts + per-peer "your vote") and `dispatch(action) -> bytes` (parses `"create <opt1> <opt2>..."` / `"vote <N>"` / `"end"` per spec §4.5.3). Pluralization: `if n == 1 { "vote" } else { "votes" }`. No-op completion-handler stubs.

**Acceptance:** `just build-fixtures` succeeds; `tests/fixtures/built/poll-interaction.wasm` exists. (No standalone tests; exercised end-to-end in T7 / T9.)

**Files touched:**
- Modify: `tests/fixtures/poll-interaction/src/lib.rs` — full impl per spec §4.1.4 + §4.5.3. Reuse counter-interaction's bump-allocator + i64-to-decimal helper (`tests/fixtures/counter-interaction/src/lib.rs:102-156`) for vote-count formatting.

**Path-A path:** modify `examples/poll/src/interaction.rs` instead.

**Implementation notes:**
- Re-read spec §4.1.4 (the exact view text format, including the "your vote" line semantics — absent if not voted; explicit "<not voted>" otherwise per spec).
- `peer_state` is 32 bytes containing the local AuthorPubkey; decode it once at the top of `view`. If `peer_state.len() != 32` (e.g., placeholder empty bytes from a pre-B-6 harness), treat as "no local vote known" — DO NOT crash.
- Pluralization is the only locale concern in v1 per spec §4.1.4 ("no i18n machinery"); the `if n == 1` ternary is enough.
- The poll prompt itself is NOT stored in state (state.options + state.creator + state.votes + state.ended only). Spec §4.1.4 view says `poll: <question/prompt>` — for v1, the "prompt" line is the literal string `"poll: <ended | in-progress>"` (no separate prompt field). Re-read §4.1.4 to confirm; if §4.1.4 actually requires a stored prompt, surface this as a spec gap before implementing (this plan flags it explicitly).

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
ls -la tests/fixtures/built/poll-interaction.wasm
cargo fmt --all -- --check
```

**Risks:** the view-text shape becomes a load-bearing surface for downstream tests (T9). Lock it in T5 by emitting a fixed-form text block; T9's assertions use `contains(...)` substring matches, NOT exact equality, to avoid coupling assertions to whitespace artifacts.

**Spec gap flagged:** §4.1.4's view sample shows `poll: <question/prompt>` but §4.2 (data model) has no prompt field on `PollState`. Two reasonable interpretations: (a) the line is a literal status line — `poll: in-progress` or `poll: ended` — and "question/prompt" is illustrative text describing the line's role; (b) the prompt is missing from the data model and should be added at genesis (`CreatePoll{ prompt: String, options: Vec<String> }`). The plan defaults to interpretation (a) — no schema change. If (b) is correct, T2 + T3 + T4 + T5 + the wire-freeze test all need touch-ups. Plan-reviewer should resolve.

---

### T6 — manifest.toml + test-utils helpers (`build_signed_poll_bundle` + `poll_handle` + `poll_bundle_three_components`)

**Spec ref:** §4.5.4 (manifest shape), §5.1 (state-tier helper instantiation pattern), §5.2 (kernel-tier helper pattern).

**Subject:** add the three poll-bundle-builder helpers to `crates/test-utils/src/bundle.rs` mirroring the counter helpers verbatim. Add `helpers::poll_handle()` to `crates/kernel/tests/helpers/mod.rs` mirroring `counter_handle()`. The manifest reuses `helpers_only_state_apply_manifest()` for the state-apply-only bundle and `helpers_only_three_component_manifest()` for the three-component bundle, both already in `crates/test-utils/src/manifest.rs`.

**Acceptance:** `cargo test -p myrhiza-kernel --test poll_state_apply` continues to pass (using the new `poll_handle()`); `cargo check -p myrhiza-test-utils --tests` is clean.

**Files touched:**
- Modify: `crates/test-utils/src/bundle.rs` — add three new pub fns (right after the existing counter ones at line 280):
  - `pub fn build_signed_poll_bundle() -> (TestBundle, BundleAddress)` (state-apply-only; mirrors `build_signed_counter_bundle` at line 127)
  - `pub fn build_signed_poll_bundle_three_components() -> (TestBundle, BundleAddress)` (state-apply + state-propose + interaction; mirrors `build_signed_counter_bundle_three_components` at line 286)
  - `pub fn poll_fixture_path() -> PathBuf` + analogous `poll_state_propose_fixture_path` + `poll_interaction_fixture_path` (mirror the existing counter_fixture_path / counter_state_propose_fixture_path / counter_interaction_fixture_path)
- Modify: `crates/kernel/tests/helpers/mod.rs` — add `pub fn poll_handle() -> StateApplyHandle` body identical to `counter_handle()` but calling `build_signed_poll_bundle()` instead.

**Path-A path:** the helpers shift to live under `crates/sdk/` once B-8 lands. For B-6 they go in `test-utils` regardless of Path-A/Path-B — B-8 will move them in its own slice.

**Implementation notes:**
- Re-read spec §4.5.4 (manifest declares state_apply + state_propose + interaction; capabilities limited to `host.log` + `host.hash` per existing `helpers_only_*` patterns).
- Mirror the file-system layout of `tests/fixtures/built/poll-*.wasm` from `tests/fixtures/built/counter-*.wasm`. The fixture paths come out of `just build-fixtures`.
- `helpers_only_three_component_manifest()` already exists in `crates/test-utils/src/manifest.rs:71` and signs with `sign_manifest_three_components` — reuse verbatim.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
cargo check -p myrhiza-test-utils --tests
cargo test -p myrhiza-kernel --test poll_state_apply
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
```

**Risks:** copy-paste errors between counter and poll helper bodies. Mitigate by reading both side-by-side in the implementer's editor before saving. The `seed` value passed to `deterministic_signing_key(seed)` must differ from counter's (counter uses 7 per bundle.rs:307) to keep the signing artifact distinct — pick seed `13` for poll.

---

### T7 — kernel-tier tests K1–K3 in `crates/kernel/tests/poll.rs`

**Spec ref:** §4.1.5 kernel-tier table (K1–K3), §5.2 (kernel-tier test infrastructure), §6.3 (non-empty-deps exercise).

**Subject:** write three in-process MemNetwork kernel-tier tests:
- K1 (`poll_e2e_single_peer_full_lifecycle`): CreatePoll → Vote → ReVote → EndPoll; assert final state-digest bytes.
- K2 (`poll_multi_author_voting`): three authors (alice/bob/carol) on the same MemNetwork bus; voters declare `deps = {creator's genesis event hash}` (NOT empty) per spec §6.3.
- K3 (`poll_unauthorized_end_poll_rejected_by_authority`): bob attempts EndPoll on alice's poll; verify the event lands in `dropped_at_apply` with verdict `Reject("EndPoll: not poll creator")`.

**Acceptance:** all three kernel-tier tests pass. `cargo test -p myrhiza-kernel --test poll` is green.

**Files touched:**
- Create: `crates/kernel/tests/poll.rs` — the three tests plus a `fast_cfg()` helper (mirrors `coexistence.rs:31` shape) and any kernel-tier helpers (e.g., `await_runtime_digest` from `coexistence.rs:50` — re-import or duplicate per coexistence.rs's pattern).

**Path-A path:** identical paths (kernel tests stay).

**Implementation notes:**
- Re-read spec §4.1.5 kernel-tier table (K1–K3) and §5.2.
- K2's non-empty-deps requirement is the LOAD-BEARING NOVEL surface. Voters' `Vote` events MUST declare `deps = {<genesis event hash>}` to exercise the non-empty-deps code path in T1+T2's decoder. Construct this by capturing the genesis event hash from `runtime_alice.author(...)`'s return value (the EventHash) and passing it in subsequent `BTreeSet::from([genesis_hash])` deps when bob/carol author their votes.
- K3 verifies `RuntimeHandle::dropped_at_apply` (already exposed per spec §4.1.5 K3 row).
- Use `MemBus::new(256)` shared across the three peers (single-peer-with-many-authors works; or three peers on the same bus — match coexistence.rs's pattern).
- Use `helpers::poll_handle()` from T6 to instantiate the state-apply.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
cargo test -p myrhiza-kernel --test poll
# expect: 3 passed; 0 failed
cargo fmt --all -- --check
cargo clippy -p myrhiza-kernel --tests -- -D warnings
```

**Determinism callout:** K2 is the cross-peer convergence canary. If any peer's `state-digest` diverges from the others, that's a CORRECTNESS bug per CLAUDE.md "Determinism is a load-bearing property" — DO NOT relax the assertion, fix the encoder.

**Risks:** non-empty deps may surface decoder bugs from T1 that the T3 state-tier tests didn't catch (T3 cases 1–6 all use empty deps). If K2 fails with `Reject("payload extends past event bytes")` or `Reject("non-empty deps not supported")`, fix the decoder in T1, not the test.

---

### T8 — coexistence test K4 (poll + counter no event crossing) — extend `coexistence.rs`

**Spec ref:** §4.1.6 (coexistence locked), §4.1.5 kernel-tier table K4 row.

**Subject:** add `poll_and_counter_coexist_no_event_crossing` to `crates/kernel/tests/coexistence.rs`, alongside the existing `two_apps_coexist_no_event_crossing`. Same shape, substituting echo's bundle for poll's. Single peer, two `Runtime`s, two distinct topics. Assert each runtime's `state-digest` progresses independently; neither runtime's `dropped_at_apply` registers the other's events; neither runtime's `peer_warnings` shows `SignatureInvalid`.

**Acceptance:** `cargo test -p myrhiza-kernel --test coexistence` is green (both the existing `two_apps_coexist_no_event_crossing` test AND the new `poll_and_counter_coexist_no_event_crossing` test pass).

**Files touched:**
- Modify: `crates/kernel/tests/coexistence.rs` — append a new test function (~80–100 lines, mirrors the existing test at line 226). The new test uses `helpers::counter_handle()` for one Runtime and `helpers::poll_handle()` for the other.

**Path-A path:** identical paths.

**Implementation notes:**
- Re-read spec §4.1.6 — the test is structurally identical to the existing one; only `helpers::echo_handle()` → `helpers::poll_handle()` substitution. Assertion shape unchanged.
- AuthorKeypair seed alignment: pick distinct seeds per app (per coexistence.rs:251-260 pattern). Suggested: counter = seed 501, poll = seed 503 (echo is 502; keeping a clean spacing).
- Poll's genesis payload is `0x00 ++ bincode(Vec<String> options)`, wrapped in a `GenesisV1` envelope per spec §4.3 — different shape from counter's `i64.to_be_bytes()` and echo's `b"hello"`.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
cargo test -p myrhiza-kernel --test coexistence
# expect: 3 passed (kernel_instantiates_and_applies_echo + two_apps_coexist_no_event_crossing + poll_and_counter_coexist_no_event_crossing)
cargo fmt --all -- --check
cargo clippy -p myrhiza-kernel --tests -- -D warnings
```

**Risks:** the existing `two_apps_coexist_no_event_crossing` is `#[allow(clippy::too_many_lines)]` per coexistence.rs:222-225; the new test will likely need the same allowance — surface this in the commit message rather than hiding it.

---

### T9 — Harness `peer_state` contract addition (B-6 §4.1.4 normative)

**Spec ref:** §4.1.4 "Harness contract addition" sub-section.

**Subject:** modify `crates/myrhiza-cli/src/lib.rs` so the harness populates `peer_state` with the local `AuthorPubkey` bytes (32 bytes) on every `view` call instead of the current always-empty `Vec::new()` at line 159. Add a regression test in `crates/myrhiza-cli/tests/e2e.rs` that uses the poll bundle and asserts the view output includes the per-peer "your vote" line after voting.

**Acceptance:**
- `cargo test -p myrhiza-cli` is green.
- The existing `counter_inc_5_inc_3_yields_final_state_8` continues to pass (counter's view ignores peer_state — its interaction component's `view` signature accepts `peer_state` but doesn't use it; counter is unaffected by the harness change).
- A new test (`poll_dispatch_displays_per_peer_vote`) creates a poll, votes, and asserts the stdout view contains a substring matching the per-peer "your vote: ..." line.

**Files touched:**
- Modify: `crates/myrhiza-cli/src/lib.rs` line 159 — replace `let peer_state: Vec<u8> = Vec::new();` with `let peer_state: Vec<u8> = author_key.author.as_bytes().to_vec();` (or equivalent — the 32-byte raw pubkey of the local author). Re-read the AuthorKeypair API at `crates/kernel/src/identity/mod.rs` to confirm field name (`author` or `public`).
- Modify: `crates/myrhiza-cli/tests/e2e.rs` — add `poll_dispatch_displays_per_peer_vote` test using `build_signed_poll_bundle_three_components()` from T6. Drive scripted stdin: `"create Yes No\nvote 0\nquit\n"` and assert stdout `contains("your vote: 0")`.

**Path-A path:** if Path-A landed, the harness contract change still lives in `crates/myrhiza-cli/` — that crate does not move with B-8. Tests file location is unchanged.

**Implementation notes:**
- Re-read spec §4.1.4 "Harness contract addition (normative — owned by this spec)" carefully. The contract says: harness MUST populate peer_state with the local AuthorPubkey on every view call. The change is intentionally unconditional — counter ignores peer_state, so it does not regress; poll uses it.
- The 32-byte raw pubkey comes from `AuthorKeypair::author` (which is `AuthorPubkey([u8; 32])` per `crates/kernel/src/identity/mod.rs`). Confirm `.as_bytes()` exists; if the field shape differs, adapt.
- The test should use `Cursor::new("create Yes No\nvote 0\nquit\n")` for stdin per the existing e2e.rs pattern (line 29 / line 71).
- This task is the only one that touches production code outside the fixtures.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just build-fixtures
cargo test -p myrhiza-cli
# expect: existing 3 tests + 1 new = 4 passed
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
```

**Determinism callout:** none — interaction is non-deterministic-OK profile (per CLAUDE.md component-profile table). The contract change does not affect state-apply or convergence.

**Risks:** AuthorKeypair API drift between B-7 and B-6. If `author_key.author.as_bytes()` is the wrong path, the build will fail loudly; adapt rather than guess. The kernel's `interaction.rs:45` already accepts `peer_state: &[u8]` — no kernel-tier change needed.

---

### T10 — gap-analysis update + docs/README.md index entries + just-ci pass

**Spec ref:** §9 (acceptance criteria — checkbox 8 marks the gap analysis update).

**Subject:** flip [`docs/reports/2026-05-21-mvp-gap-analysis.md`](../reports/2026-05-21-mvp-gap-analysis.md) implementation.md §20 item 16 from ❌ to ✅ + add catalog entries for the B-6 spec and plan to [`docs/README.md`](../README.md) under "Runtime core" (where B-5 / B-7 live). Run `just ci` to verify the full gate.

**Acceptance:** `just ci` is green (fmt + clippy + tests + tests-iroh + spec-coverage); `git diff docs/reports/2026-05-21-mvp-gap-analysis.md` shows item 16 marked ✅ with a `Shipped in B-6 (2026-05-26)` annotation; `git diff docs/README.md` shows two new catalog lines under "Runtime core".

**Files touched:**
- Modify: `docs/reports/2026-05-21-mvp-gap-analysis.md` — at line 64 (the B-6 section), update from "deferred slice; not v1-blocking" with ❌ markers to ✅. Re-read the existing entry to determine the precise update shape; preserve the "not v1-blocking" framing because the spec confirms B-6 is post-criteria-4-closure.
- Modify: `docs/README.md` — add two lines under "Runtime core" (after line 36):
  - `- [Plan B-6 — Poll app v1 design](specs/2026-05-26-b-6-poll-app-design.md) — second MVP demo app: multi-author voting, creator-only EndPoll permission gate, first fixture tolerating non-empty deps, first non-empty peer_state. Realized by [plan B-6](plans/2026-05-26-b-6-poll-app.md). \`[draft]\``
  - (Plan line is part of the spec line via the embedded link — match the precedent in B-1's entry at line 33.)

**Path-A path:** identical paths.

**Implementation notes:**
- Re-read CLAUDE.md "Branch & PR Hygiene" + organizing-docs skill (catalog entries are required for every new spec/plan; commit doc + README entry together — see organizing-docs §"Adding a new spec, plan, or report").
- The README entry style matches the precedent on line 33 (`Plan B-1 — Event DAG + MemNetwork`). One catalog line per logical entry.
- `just ci` runs the full gate including `spec-coverage` — that check regenerates `tests/spec-coverage.md`; if it shifts, commit the regenerated file.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
just ci
# expect: green
grep -c "Plan B-6 — Poll app v1 design" docs/README.md  # expect: 1
grep "item 16" docs/reports/2026-05-21-mvp-gap-analysis.md  # expect: ✅ marker visible
```

**Risks:** the gap-analysis file's exact wording at line 64 may have drifted since spec authoring. Re-read the report and adapt — the principle (flip ❌ → ✅) is what matters; the surrounding prose is editor's discretion.

---

### T11 — Final commit-message lint pass + branch-wide review prep

**Spec ref:** N/A (process step, mirrors B-7 plan's "After all 8 slices" step).

**Subject:** verify every commit message on `feat/b-6-poll-app` follows Conventional Commits (`feat:` / `fix:` / `docs:` / `refactor:` / `chore:`) per CLAUDE.md "Branch & PR Hygiene". Ensure no `--no-verify`, no force-push to main. Confirm the final state of the working tree is clean.

**Acceptance:**
- `git log --oneline main..HEAD` shows one commit per task T0–T10 (11 commits), each prefixed with a Conventional-Commits type.
- `git status` is clean.
- `just ci` is green from a fresh clone of the branch.

**Files touched:** none (process-only task; possibly squash-fix commit messages via `git rebase -i` if needed — but only with maintainer approval per CLAUDE.md "Never use git commands with the -i flag").

**Implementation notes:**
- This is a discipline check, not new work. If a commit message is non-compliant, the implementer should NOT use `--no-verify` to bypass hooks; instead, write a fixup commit per CLAUDE.md guidance.
- If `just ci` fails after T10, root-cause and fix per CLAUDE.md "Root-cause every bug" — DO NOT skip the failure.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
git log --oneline main..HEAD
just ci
git status
```

**Risks:** none structural; this is a final lint pass.

---

### T12 — Final-review prep + PR draft

**Spec ref:** N/A (process step, mirrors B-7 plan's final-review step).

**Subject:** dispatch a fresh `requesting-code-review` agent across the entire branch (B-6's 11 commits) before opening a PR. Resolve any final findings via targeted commits. Open the PR with a summary that links to the spec and plan.

**Acceptance:** PR open on GitHub against `main`; PR description summarizes the 11 tasks and cites both spec + plan; CI passes on the PR.

**Files touched:** none (PR creation only).

**Implementation notes:**
- Re-read CLAUDE.md "Branch & PR Hygiene" — one concern per PR. If the final-review surfaces a side issue (e.g., a refactor that touches B-7 code), file it as a follow-up, do not bundle.
- Cite the spec + plan in the PR body. Mark which path (A or B) was taken at T0.
- DO NOT mark criterion 3 ✅ in the gap analysis — that's B-7's claim. Mark only implementation.md §20 item 16.

**Verification commands:**

```bash
cd /mnt/storage/projects/myrhiza/.claude/worktrees/b-6-poll-app
gh pr create --title "feat: plan B-6 — poll app v1" --body "<see body block>"
# wait for CI to pass on the PR before merging
```

**Risks:** if final-review surfaces a determinism gap, treat it as a correctness bug per CLAUDE.md, not a scope-creep avoidance.

---

## Spec-coverage table

| Task | Spec section closed |
|---|---|
| T0 | §3 (layout: Path-A vs Path-B contingency), §4.5.4 (manifest scaffolding scope) |
| T1 | §4.5.1 (apply skeleton — decoder + match scaffolding), §6.3 (non-empty-deps tolerance) |
| T2 | §4.2 (data model / PollState), §4.3 (event vocabulary), §4.5.1 (apply impl + encoder), §4.1.2 (last-vote-wins + BTreeMap), §4.1.3 (creator gate), §6.1 (determinism trapdoor) |
| T3 | §4.1.5 state-tier table (cases 1–6 + 6b + 7–10), §5.1 (state-tier infrastructure) |
| T4 | §4.5.2 (state-propose impl) |
| T5 | §4.1.4 (view shape — all four projections + peer_state contract narrative), §4.5.3 (interaction impl + dispatch parser + pluralization) |
| T6 | §4.5.4 (manifest), §5.1 (state-tier helper instantiation), §5.2 (kernel-tier helper pattern) |
| T7 | §4.1.5 kernel-tier table K1–K3, §5.2 (kernel-tier infrastructure), §6.3 (non-empty-deps exercise — K2) |
| T8 | §4.1.6 (coexistence with counter), §4.1.5 K4 |
| T9 | §4.1.4 "Harness contract addition" sub-section (normative) |
| T10 | §9 (acceptance criteria checkbox 8 — gap-analysis update) |
| T11 | (process) — CLAUDE.md Branch & PR Hygiene |
| T12 | (process) — final-review + PR |

## Determinism callouts (per-task)

- **T2** — BTreeMap canonical encoder is load-bearing for cross-peer convergence (spec §6.1 + §4.1.2 "BTreeMap, not HashMap"). Any HashMap is a CORRECTNESS bug per CLAUDE.md "Determinism is a load-bearing property". Float-Display creep risk: `cargo build` for the fixture must NOT emit `f32.*`/`f64.*` instructions; spot-check via `wasm-tools dump`.
- **T3** — golden-bytes assertion on the initial `PollState` is the wire-freeze canary. If a future PR drifts the canonical encoding, T3 catches it immediately (spec §4.5.1 last paragraph).
- **T7** — K2 (multi-author voting) is the cross-peer convergence canary at the kernel tier. If `state-digest` diverges across peers, fix the encoder, not the assertion.
- **T9** — interaction profile is non-deterministic-OK per CLAUDE.md component-profile table. No determinism callouts.
- **All others** — no fresh determinism surface; relying on T2's encoder invariant.

## Risks per task (where non-obvious)

- **T1 — non-empty-deps decoder bug:** see T1 "Risks" block. Dynamic PAYLOAD_LEN_OFFSET. Sanity-check both `deps_len = 0` and `deps_len = 1` envelopes manually.
- **T2 — float-Display creep:** wasm fixture's panic-format paths can pull in `f64::Display`; the kernel's float-ban byte-scan rejects at instantiation. Spot-check via `wasm-tools dump | grep f64\\.` (see T2 verification snippet).
- **T3 — encoder-replica drift:** the test-side `encode_poll_state` must match the fixture's encoder byte-for-byte. Factor the format into a small helper.
- **T5 — spec gap on poll prompt:** §4.1.4 view text shows `poll: <question/prompt>` but §4.2 data model has no prompt field. Plan defaults to "status line as literal" (interpretation a) — if wrong, T2/T3/T4/T5 + wire-freeze test all need touch-ups. **Flagged for plan-reviewer.**
- **T7 — non-empty deps surfaces decoder bugs:** if K2 fails with payload-out-of-range errors, fix the T1 decoder, not the test.
- **T9 — AuthorKeypair API path:** `author_key.author.as_bytes()` may differ; adapt to actual field shape.

## Estimate breakdown

| Task | Days | Notes |
|---|---|---|
| T0 | 0.20 | mostly mechanical (boilerplate copy from counter); decision check is cheap |
| T1 | 0.25 | non-empty-deps decoder is the novel surface (~15 LOC change vs counter); needs sanity-check |
| T2 | 0.50 | hand-rolled BTreeMap encoder (~30 LOC) + three discriminator arms + genesis path |
| T3 | 0.50 | 10 state-tier tests + test-side encoder replica + envelope helpers |
| T4 | 0.25 | propose mirrors counter's shape; ~60 LOC body |
| T5 | 0.35 | interaction view formatting + dispatch parser; the most LOC of any component (~150) |
| T6 | 0.15 | test-utils helpers (copy-paste from counter pattern) |
| T7 | 0.40 | three kernel-tier tests; K2 non-empty-deps construction is the trickiest |
| T8 | 0.15 | coexistence test (mirror of existing two_apps_coexist) |
| T9 | 0.20 | one-line harness change + one new e2e test |
| T10 | 0.10 | docs updates + just-ci pass |
| T11 | 0.05 | lint pass |
| T12 | 0.10 | PR open + final-review dispatch |
| **Total** | **3.20 d** | upper bound of spec's 2.5–3.0 d range — 0.2 d slack absorbed |

Spec §8 estimate is 2.5–3.0 days; this breakdown rounds to 3.0 d nominal with 0.2 d cushion for the non-empty-deps decoder debug pass that spec §6.3 names. The slack lives in T1/T7 — if the decoder is straightforward in T1, T7 won't burn its full budget either.

## Self-review

- **Spec coverage:** every spec section §3, §4.1.1–§4.1.6, §4.2, §4.3, §4.4, §4.5.1–§4.5.4, §5.1, §5.2, §5.3, §6.1–§6.6, §8, §9 has a task entry in the spec-coverage table. §6.x risks are flagged inline as "Risks per task". §10 (sources) is informational, no task needed.
- **Placeholder scan:** no "TBD" / "implement later" / "similar to Task N (without code)". Every task has concrete files + concrete commands.
- **Type consistency:** `PollState` fields (`creator`, `options`, `votes`, `ended`) match across T2 / T3 / T4 / T5. Helper names (`poll_handle`, `build_signed_poll_bundle`, `build_signed_poll_bundle_three_components`) consistent across T6 / T7 / T9. WASM artifact paths (`tests/fixtures/built/poll-*.wasm`) consistent across T0 / T1 / T2 / T4 / T5 / T6 / T9.
- **Ordering check:** T0 (scaffold) → T1 (skeleton, builds) → T2 (impl, builds) → T3 (state-tier tests, requires T2 + T6 helper). T6 lands AFTER T3 because T3's helper `poll_handle()` lives in `helpers/mod.rs` and uses `build_signed_poll_bundle()` from T6. This is a known crossing — to break it, T6 (the test-utils helper) could land before T3 (the tests). **Adjustment:** move T6 logically *before* T3 in execution order. Numbering is preserved (re-numbering ripples through downstream cross-refs); the implementer should execute T6 immediately after T2 and before T3. Flagged here for the implementer.
- **Falsifiable acceptance:** every task has a `cargo test ... --test <name>` or `just ci` command. No "looks reasonable" assertions.

## Execution ordering hint (per self-review)

Despite the T0–T12 numbering, the natural data-dependency order is:

```
T0 → T1 → T2 → T4 → T5 → T6 → T3 → T7 → T8 → T9 → T10 → T11 → T12
```

T6 (test-utils helpers) is a prerequisite for T3 (state-tier tests) because T3 invokes `helpers::poll_handle()` which builds via `build_signed_poll_bundle()`. The implementer can preserve the T0–T12 numbering by running them in this order, or re-number on-branch — either is fine. The plan-reviewer should confirm the ordering before impl-start.

## Cross-references

- Parent spec: [docs/specs/2026-05-26-b-6-poll-app-design.md](../specs/2026-05-26-b-6-poll-app-design.md)
- Precedent plan (recent, similar shape): [docs/plans/2026-05-22-e2e-test-coverage.md](2026-05-22-e2e-test-coverage.md)
- Precedent plan (B-7, interaction harness): [docs/plans/2026-05-21-plan-b-7.md](2026-05-21-plan-b-7.md)
- Fixture templates: `tests/fixtures/counter-state-apply/`, `counter-state-propose/`, `counter-interaction/`, `echo-state-apply/`
- Test-utils helpers (already exist): `crates/test-utils/src/bundle.rs:127` (`build_signed_counter_bundle`), `crates/test-utils/src/bundle.rs:286` (`build_signed_counter_bundle_three_components`), `crates/test-utils/src/manifest.rs:71` (`helpers_only_three_component_manifest`)
- B-7 harness ABI surface (the contract this plan extends in T9): `crates/myrhiza-cli/src/lib.rs:159`
- Build infrastructure: [`Justfile`](../../Justfile)
- Docs catalog: [`docs/README.md`](../README.md) (T10 updates this)
- Gap analysis: [`docs/reports/2026-05-21-mvp-gap-analysis.md`](../reports/2026-05-21-mvp-gap-analysis.md) (T10 updates this)
